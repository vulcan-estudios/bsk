/**
 * API Connection
 *
 *
 * GET:
 *
 * Api.get('endpoint).done(function(rs) {
 *
 * }).fail(function() {
 *
 * }).always(function() {
 *
 * });
 *
 *
 * POST:
 *
 * Api.post('endpoint', { data }).done(function(rs) {
 *
 * }).fail(function() {
 *
 * }).always(function() {
 *
 * });
 *
 */

module.exports  = {

};